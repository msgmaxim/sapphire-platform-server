const assert = require('assert')

module.exports = {
  runTests: function(platformApi) {

    // needs that first post...
    /*
    it('get post reples', async () => {
      const res = await platformApi.serverRequest('/posts/1/replies')
      assert.equal(200, res.statusCode)
      // right now just []
      console.log('replies res', res.response.data)
    })
    */
  },
}
